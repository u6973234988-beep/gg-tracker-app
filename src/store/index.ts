// Zustand store per lo stato globale dell'applicazione
// Negozi verranno aggiunti qui durante lo sviluppo

// Esempio di store (da decommentare e adattare):
/*
import { create } from 'zustand';

interface AppState {
  user: any | null;
  loading: boolean;
  setUser: (user: any) => void;
  setLoading: (loading: boolean) => void;
}

export const useAppStore = create<AppState>((set) => ({
  user: null,
  loading: false,
  setUser: (user) => set({ user }),
  setLoading: (loading) => set({ loading }),
}));
*/
